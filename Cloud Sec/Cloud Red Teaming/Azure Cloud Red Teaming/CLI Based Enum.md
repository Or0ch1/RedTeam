To check if an organization is using Entra ID or an Identity provider (IDP) we use the below URL and replace the domain

``` 
https://login.microsoftonline.com/getuserrealm.srf?login=Username@DomainName&xml=1
```

